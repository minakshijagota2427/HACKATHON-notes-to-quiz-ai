services:
  - type: web
    name: notes-to-quiz-backend
    env: python
    buildCommand: |
      pip install -r backend/requirements.txt
      apt-get update
      apt-get install -y tesseract-ocr
    startCommand: python backend/app.py